# ch3 基线锁死分析记录

数据：`Ozone_DataSampling_260920.csv`（127.39 s，≈1000 Hz，127390 行）
固件：50 Hz 陷波版（`adc_diag_version = 20260921`）
分析脚本：`build/_ch3focus.mjs`、`build/_ch3events.mjs`、`build/_csv_an1..3.mjs`

---

## 1. 现象（用户描述）

> 我连续敲击之后电压值向上突变并且无法恢复原来的基准

## 2. 数据里到底发生了什么

`adc_notch_vin[3]` 在 **t = 73.8161 s 的单个 1 ms 采样内**跳变，之后 47.4 s 完全不再恢复。

| 时刻 | raw[0] | raw[1] | raw[2] | raw[3] |
|---|---|---|---|---|
| 73.8151 | 39 | 213 | 116 | 1674 |
| **73.8161** | **728** | **4095** | **4095** | **4095** |
| 73.8171 | 433 | 4095 | 4095 | 2892 |
| 74.0 | ~426 | 4095 | 4095 | ~2500 |
| 80–127 s | 423 | 4095 | 4095 | 2620 |

* 四个通道在同一个采样点同时撞到满量程 → 共同环节（基准/供电/模拟前端）被推动。
* ch1、ch2 之后 **100 % 的样本锁在 4095**（47.4 s 无一例外）。
* ch0、ch3 上移后不再回来。

### ch3 分段统计

| 区间 | 均值 | min | max | std | p-p |
|---|---|---|---|---|---|
| notch_vin[3] 前(10–71 s) | 1.5736 V | 0.012 | 3.371 | 0.189 V | 3.359 |
| notch_vin[3] 后(80–126 s) | 2.1088 V | 1.640 | 2.559 | 0.149 V | 0.919 |

**台阶 = +537 mV。**

### 恢复速率

| 事件后 | 均值 | 相对锁死前 |
|---|---|---|
| +0–5 s | 2.1101 V | +536.9 mV |
| +20–25 s | 2.1043 V | +531.1 mV |
| +50–55 s | 2.1020 V | +528.8 mV |

线性趋势斜率 = **−0.392 mV/s**。

* 50 s 只回来 **19.6 mV = 台阶的 3.6 %**。
* 按此速率线性外推，恢复 537 mV 需要 **22.8 分钟**。
* 该斜率基本就是噪声级 —— 说明**没有有效的泄放通道**，电荷只能靠 INA 约 0.3 nA 的输入偏置电流和板级漏电慢慢漏。

## 3. 换算到放大器输入端

`Vout = VREF + G·(VIN+ − VIN−)`，`G = 1 + 49400/510 = 97.9`，`VREF = 2.5 V`。

* 537 mV 台阶 ÷ 97.9 = **输入端只漂了 5.5 mV**。
* 静止时 ch3 输出 1.5736 V → `VIN+ = 2.4905 V`，比 `IN− = 2.500 V` **低 9.5 mV**，×97.9 = 0.93 V 的静态输出偏置。
* 也就是说：**传感器静态输出（≈2.490 V）和 IN− 的硬 2.500 V 不匹配**，这 9.5 mV 已经吃掉了大半输出摆幅。

## 4. 50 Hz 陷波的实际效果 —— 基本无效

前段（10–71 s）频谱：

| | 50 Hz 带(47–53) | 100 Hz 带 | 150 Hz 带 | 1–490 Hz 总 |
|---|---|---|---|---|
| vin[3] 陷波前 | 24.6 mVrms | 24.0 | 24.1 | **216.0 mVrms** |
| notch_vin[3] 陷波后 | 13.8 mVrms | 23.6 | 23.9 | **214.4 mVrms** |

* 50 Hz 只有 **−5 dB**，仿真里是 −73 dB，差 68 dB。
* 100 Hz、150 Hz **完全没变**。
* 谱峰前六名：36.7 / 114.9 / 21.2 / 180.1 / 147.2 / **50.9** Hz，幅度只有 4–5 mV。
* 47–53 Hz 带内的 24.6 mVrms ≈ 216 mVrms 白噪声在该带宽内的预期值（≈24 mVrms）。

**结论：ch3 里没有稳定的 50 Hz（或倍频）正弦，只有宽带噪声。这份数据不支持“工频耦合”的判断，陷波器在陷一个不存在的东西。**

## 5. 噪声是混叠的

* `notch_vin[3]` 相邻 1 ms 样本的平均跳变 = **252 counts = 0.20 V**（std 0.189 V）。
* 相关时间 < 1 ms，在 1 kHz 采样下表现为白噪声 → 真正的高频成分在 500 Hz 以上被折叠进来。
* 成因：ADC 采样时间只有 1.5 周期，放大器输出到 ADC 之间**没有模拟抗混叠滤波器**。
* 软件滤波无法还原，必须在 ADC 前加模拟低通。

## 6. 每次敲击都把放大器打到两个轨

63.7 s 安静段内，`|notch_vin[3] − 中位| > 0.6 V` 的事件共 **430 次**（≈6.8 次/秒）。

幅度分布：0.6–0.8 V × 280，0.8–1 V × 55，1–1.5 V × 54，1.5–2 V × 41。

典型波形（t = 70.715 s）：

| t(s) | raw[3] | 说明 |
|---|---|---|
| 70.7151 | 38 | 撞下轨 |
| 70.7161 | 4095 | 1 ms 后撞上轨 |
| 70.7171 | 2454 | 回落 |

**相邻 1 ms 内从 0.03 V 甩到 3.30 V。** G = 97.9 时输出摆幅 ±1.7 V 只对应输入 ±17 mV，敲一下必然削顶。

## 7. 锁死那一下和前面 429 次长得一模一样

t = 73.8161 的波形（下轨 0.0306 V）→ 73.8171（上轨 2.9599 V）与 t = 70.7151/70.7161 的普通敲击事件**完全相同**。区别只在于：这一次电荷积累越过了阈值，直流工作点再也没回来。

## 8. 陷波滤波器是无辜的

* 锁死后 `notch_vin[3]` 与 `vin[3]` 的直流差 = **0.070 mV**。
* 锁死瞬间两者差 ±100 mV 的摆动，是 Q = 10 双二阶在 50 Hz 的振铃，`τ = Q/(π·f₀) = 64 ms`，几十毫秒就衰减完，属正常行为。
* 线性稳定 IIR 不可能产生永久直流偏移；`adc_raw`（未经任何滤波）也在同一采样点跳变，证明跳变在模拟域、ADC 之前。

---

## 9. 硬件修改建议（根因）

1. **给压电输入加直流泄放电阻**：每个压电片两端并 1–10 MΩ。`C_piezo ≈ 10 nF`、`R = 10 MΩ` → `τ = 0.1 s`，敲击电荷 0.1 s 内泄掉，基线不会再锁死。**这是解决“敲完回不去”最直接的一招。**
2. **对齐或隔断输入端直流**：
   * 方案 A（推荐）：传感器输出串电容隔直，再用电阻把 IN+ 偏置到**与 IN−、REF 同一个 2.5 V 节点**。静止差分 = 0 → 输出正好 2.5 V，上下各 1.7 V 对称摆幅，积累的直流根本上不了放大器。下限 1 Hz 取 R = 1 MΩ / C = 1 µF；下限 5 Hz 取 R = 1 MΩ / C = 0.22 µF。
   * 方案 B：把 IN− 从硬接 2.5 V 改为可调，调到与传感器实际静态输出（≈2.490 V）一致。
3. **降低增益**：G = 97.9 只有 ±17 mV 输入范围。建议 **G = 10–20**（G = 20 → Rg = 2.6 kΩ；G = 10 → Rg = 5.49 kΩ）。
4. **加模拟抗混叠低通**：RC 或二阶 Sallen-Key，截止 200–500 Hz，放在放大器输出与 ADC 之间。
5. **保护 ADC 引脚**：放大器 5 V 供电、输出可达 ~4.9 V，STM32 ADC 上限是 VDDA = 3.3 V。数据里 ch1/ch2 的引脚已在 4095 上被钳位 47 s。串 1–10 kΩ + 肖特基钳位到 3.3 V，或把放大器供电降到 3.3 V。

### 30 秒判别测量（板子现在还锁着，正好测）

| 测点 | 数据预测值 |
|---|---|
| 5 V LDO 输出 | 5.00 V |
| 2.5 V 基准（REF 与 IN−） | 2.50 V |
| ch3 放大器 IN+ | ≈2.496 V |
| ch3 放大器 OUT | ≈2.11 V |

* VCC、REF 都正常 → 传感器输入端直流漂移（电荷无处泄放）→ 做第 1、2 条。
* REF 或 VCC 不对 → 基准/供电故障（敲击把虚接或接插件震松了）。

---

## 10. 固件兜底改动（治不了根，但让系统可用、可诊断）

文件：`Core/Src/main.c`，`adc_diag_version` 已升到 **20260922**。

| 变量 | 作用 |
|---|---|
| `ch_rail[4]` | 连续 20 ms 撞轨 → 判定该路锁死 |
| `ch_rail_ms[4]` | 当前已连续贴轨多少 ms |
| `ch_rail_events` | 进入锁死的总次数 |
| `osc_amp_ch[4]` | 每路各自的振荡幅度，用来找“哪一路还活着” |
| `trigger_ch` | **可写** 0–3，选哪一路做振荡触发（默认 0） |
| `trigger_ch_used` | 实际生效的触发通道（选中路贴轨时自动换到幅度最大的一路） |
| `peak_up[4]` | 抓峰窗口内相对基线的最大**上**偏量 |
| `peak_dir` | **可写** 0 = 位置用下偏 peak[]（原行为），1 = 用上偏 peak_up[] |
| `hit_valid` | 1 = 上次触发 ≥3 路可用，位置可信 |
| `hit_rejected` | 因贴轨通道过多而被丢弃的触发次数 |
| `adc_rezero` | **可写 1**：立刻重采基线并复位滤波器，不用断电 |

行为改动：

1. 贴轨通道**整体冻结**（不滤波、不跟踪基线）。原代码会让 `base[]` 一路爬到 3.3 V，把锁死的通道伪装成正常通道。
2. 通道从轨上下来时自动重播种并复位陷波状态，台阶不会被当成一次击打。
3. `hit_x`/`hit_y` 排除贴轨通道；可用通道 < 3 路时丢弃本次触发并累加 `hit_rejected`，不再输出假坐标。
4. **重要**：原来 `osc_update()` 和 strike 检测**都只读 ch0**。本份数据里 ch0 均值 0.031 V、100 % 贴在 0.03 V 下轨、1 ms 抖动仅 0.4 mV（对照 ch3 为 0.20 V）—— 等于整个检测挂在一路死通道上。这是“怎么敲 `strike_count`/`hit_count` 都不动”的直接原因。现在可用 `trigger_ch` 切到 ch3，选中路贴轨时也会自动退回。

编译：FLASH 17076 B / 32768 B (52.11 %)，RAM 2440 B / 4096 B (59.57 %)。
产物：`build/Debug/Pressure_Test.elf`。

---

# 附录：`strike_count` 未击打也自增的分析与修复（fw 20260923）

## A11. 两个计数器的判据

### `strike_count`（原 `main.c:426-437`）
```c
if (vin_f[tc] > base[tc] + STRIKE_MARGIN)   // STRIKE_MARGIN = 0.10 V
    strike_hold = STRIKE_HOLD_MS;           // 100 ms
striking = (strike_hold > 0);
if (striking && !strike_prev) strike_count++;   // 上升沿 +1
```
只看向上越界 0.10 V。`vin_f` = 陷波后再过 α=0.1 低通（≈16 Hz）。

### `hit_count`（原 `main.c:439-484`）
必须同时满足：`armed==1` ∧ `osc_floor > 0.85 V` ∧ 30 ms 窗口内四路 `peak[]` 之和 `Sp > 0.001 V`。
`osc_floor = min(osc_amp, osc_floor + 0.001)`，即"20 ms 峰峰值的近期下界"，只能以 1 mV/ms 上回。
复位条件 `osc_floor < 0.70`。`HIT_THRESHOLD 0.3f` 定义了但从未使用。

## A12. 回放：不敲也计数

把上述逻辑原样跑在 `adc_notch_vin[3]` 上（`build/_replay_hitcount.mjs`、`build/_replay2.mjs`）：

| 段 | 触发通道 | `hit_count` | `strike_count` |
|---|---|---|---|
| 静置 5–73.7 s (68.7 s) | ch0 | 0 (0/min) | 0 (0/min) |
| 静置 5–73.7 s | **ch3** | +3 (2.6/min) | **+92 (80.4/min)** |
| 锁死后 80–127 s | ch3 | 0 | +4 (5.1/min) |

空闲时 `osc_floor` 分位（阈值 0.850）：ch3 p50=0.604 **p90=0.690** p99=0.791 **max=0.919**；ch0 p50=0.001。

**两个阈值（0.85 触发 / 0.70 复位）都落在 ch3 空闲噪声的分布里面**，状态机自己在两个阈值之间来回翻转。

## A13. 记录里根本不存在敲击信号

`build/_shape_probe.mjs` 对 `raw[3]` 做的形态分析：

* 偏离中位 >0.5 V 的事件 **979 个，时长 p50 = 0.0 ms、p90 = 0.0 ms、max 7 ms** → 全是单采样脉冲。
* 偏离 >2 V 的事件 **0 个**。
* `raw[3]` 大偏离时同时偏离的通道：ch1 **18.4 %**、ch2 **8.6 %**、ch0 **0.1 %** → 不是共同事件。
* 事件到达率 **5.1 次/秒**，间隔 p50 = 0.153 s。

真敲击是机械事件：四角应同时响应、持续数毫秒。以上三条都不满足 ⇒ **`strike_count` 数的 100 % 是噪声。**

## A14. 为什么"抬阈值"救不了（`build/_validate_adaptive.mjs`）

| 阈值 | 误触发 |
|---|---|
| 固定 0.10 V | 77.8 /min |
| 自适应 4σ (0.139 V) | 9.6 /min |
| 自适应 6σ (0.208 V) | 11.4 /min |
| 自适应 **10σ (0.347 V)** | **8.7 /min** |

阈值抬到 **10σ** 仍有 8.7/min ⇒ 噪声是**脉冲型而非高斯型**，单路幅度阈值分不开。
更糟：真敲击事件在 `vin_f` 里的峰值 p50 = **0.069 V**、max = **0.124 V**，比噪声尖峰还小。

## A15. 能分开的是"同时性"（`build/_coincidence.mjs`）

| 判据 | 误触发 |
|---|---|
| 固定 0.10 V，单通道（= 原固件） | 336 /min |
| 自适应 6σ，单通道 | 325 /min |
| 自适应 6σ + 同时 2 路 (±5 ms) | 43.7 /min |
| **自适应 6σ + 同时 3 路 (±5 ms)** | **0 /min** |
| 自适应 10σ + 同时 3 路 (±5 ms) | **0 /min** |

## A16. 固件改动（fw 20260923）

| 变量 | 作用 |
|---|---|
| `noise_sigma[4]` | 每路 `vin_f-base` 的在线噪声 σ（σ 裁剪 EMA，只用 \|d\|<3σ 的安静样本，α=0.0005、τ≈2 s） |
| `strike_thr[4]` | 每路实际阈值 = `clamp(6σ, 0.10, 1.20)` V，**下限保持 0.10 保证不比原来更敏感** |
| `strike_dev[4]` | 本路当前向上偏离量 |
| `strike_ch[4]` | 本路本周期是否越界 |
| `strike_coinc` | 最近 5 ms 窗口内同时越界的路数 0–4 |
| `strike_reject` | 因不同时而被丢弃的候选数 |
| `osc_floor_ref` / `osc_floor_spread` / `osc_floor_thr` | `osc_floor` 的自适应参考值、离散度、当前触发阈值 |
| `trigger_ch` | **默认 4 = 自动**选幅度最大且未贴轨的一路；写 0–3 可固定 |

行为改动：
1. `strike_count` 改为 **每路自适应 6σ + 至少 3/4 路在 ±5 ms 内同时越界**，并有 100 ms 不应期。
2. `hit_count` 触发改为 `osc_floor > osc_floor_ref + 6×osc_floor_spread`；复位点取"参考→阈值"的中点，避免阈值附近翻转。
3. `hit_count` 额外要求 **≥3 路的 `peak[]` 超过各自噪声的 4 倍**（同样是同时性原则）。
4. **触发通道默认自动**：原代码两套检测都只读 ch0，而 ch0 空闲时 1 ms 抖动只有 0.4 mV。

## A17. 验证（`build/_verify_new.mjs`，把新逻辑原样回放到同一份 CSV）

| 段 | 旧逻辑 20260921 | 新逻辑 20260923 |
|---|---|---|
| 静置 5–73.7 s | strike +0，hit +3 | **strike +0，hit +0** |
| 锁死后 80–127 s | strike +0，hit +0 | strike +0，hit +0 |
| 全程 5–127 s | strike +60 (29.5/min)，hit +3 | **strike +1 (0.5/min)，hit +0** |

> 注：过程中发现把阈值下限设成 0.05 V 会让静置段回升到 12.2/min —— 因为"死通道"的 σ 会塌到下界，
> 于是用 0.05 V 去判它偶然的尖峰。下限必须是 0.10 V。回放里保留了这个对照。
> 全程剩下的那 1 次落在 73.8 s 的锁死瞬间 —— 四个通道确实同时发生了巨大变化，判为一次事件是正确的。

编译：FLASH 18472 B / 32768 B (56.37 %)，RAM 2944 B / 4096 B (71.88 %)。
产物：`build/Debug/Pressure_Test.elf`。

**注意：这一层只是让计数不再说谎。噪声本底不降下来，任何判据都只能靠"同时性"这一条物理差异硬撑，
灵敏度必然受限。必须做的仍是第 9 节的硬件修改（抗混叠、降增益、输入泄放/隔直、ADC 保护）。**
