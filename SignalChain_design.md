# 压力测试板 —— 定制滤波器方案

依据数据：`Ozone_SecondTest.csv`（按压，14.90 s）、`Ozone_ThirdTest.csv`（击打，14.31 s）、
以及早前的 `Ozone_FirstTest.csv`（99.6 s 静置）。
所有数字都是在这三份真实数据上实测的，不是估算。

---

## 1. 数据告诉我们什么

### 1.1 共同前提：噪声是白的

三份数据的静置段噪声功率谱都满足 **各频带 RMS ÷ √带宽 ≈ 常数**，从 0.5 Hz 一直到 500 Hz：

| 文件 | 0.5-2 Hz | 2-10 | 10-30 | 30-100 | 100-200 | 200-400 | 400-500 |
|---|---|---|---|---|---|---|---|
| SecondTest | 12.5 | 16.3 | 17.1 | 16.9 | 17.2 | 17.6 | 16.8 |
| ThirdTest | 15.7 | 15.4 | 16.7 | 15.7 | 16.9 | 17.1 | 17.3 |

（单位 mVrms / √Hz）。**平坦白噪声，单点 σ ≈ 311–316 mV。**

这一条决定了整个方案：对白噪声，滑动平均就是最优线性滤波器，`σ_out = σ_in / √N`。

### 1.2 按压 SecondTest —— 有信号

| 量 | 实测值 |
|---|---|
| 静置基线 | 0.5249 V |
| 原始单点噪声 σ | 316 mV |
| 事件区间 | t = 3.372 … 7.214 s |
| **按压幅度** | **+285 mV，方向向上** |
| 起始沿 10%→90% | **约 1.2 s**（很慢，见下） |
| 释放指数衰减 τ | **1.95 s** |
| 持续时长 | 约 3.5 s |

### 1.3 击打 ThirdTest —— **没有信号**

用 300 点平滑做参考，用 5σ 判据扫全程：**0 个持续事件**。
用 100 点平滑（σ=37.4 mV）扫全程：**最大偏离 111 mV = 3.0σ**。

对比：14000 个高斯白噪声样本的理论极值约 3.5–4σ。**3.0σ 比噪声本身还小。**

每秒的"最大单点偏离"是 794–1085 mV，而 3σ = 930 mV —— 每一秒的最大值都正好是白噪声的理论极值，没有任何一秒例外。

> **结论：这次击打没有在电子学上留下任何痕迹。** 滤波器救不了一个不存在的信号。
> 需要先确认：击打有没有真的打到传感器？传感器和按压用的是同一路吗？放大器是不是把冲击削掉了？

### 1.4 这两条事实决定了滤波器该怎么设计

1. **噪声白** → 平均就是最优，`σ_out = σ_in/√N`，不需要陷波器，不需要中值滤波
2. **按压的起始沿有 1.2 秒** → 可以用极重的平滑而几乎不损失响应。实测等效 N=500 仍保留 **98%** 的峰值，N=1000 保留 94%
3. **击打是冲击型（单采样脉冲）** → 平均会毁掉它：信号降到 1/N，噪声只降到 1/√N，**SNR 反而变差 √N 倍**

第 3 条是关键：**按压和击打需要相反方向的滤波器**，不可能用一条链同时兼顾。

---

## 2. 方案

### 2.1 主链（力 / 按压）

```
adc_raw[0] @ 1 kHz
   │
   ├── 第1级 20 点滑动平均  →  50 Hz      σ: 316 → 70.2 mV   RAM 40 B
   │
   ├── 第2级 25 点滑动平均（在 50 Hz 流上）  σ: 70.2 → 9.7 mV   RAM 50 B
   │      输出等效 = 500 点平均，群延迟 ≈ 250 ms
   │      N 取 20 的倍数 → 50/100/150/200 Hz 精确零点，工频全灭
   │
   ├── 基线 + σ 在线估计（50 Hz 上跑；按下期间完全冻结）
   │
   ├── dev = 滤波值 − 基线        ★ 按压为【正】
   │
   └── 双阈值滞回：dev > 6σ → 按下    dev < 3σ → 释放
```

**为什么用级联而不是单级 N=500**：真实数据上两者输出 σ 分别 20.54 mV / 20.64 mV，
相关系数 **0.988**，完全等效；但 RAM 是 **90 B vs 1000 B**。
抽取带来的混叠代价实测只有 0.7 mV（一级后 70.2 mV vs 理论 69.5 mV）。

**为什么不用 IIR 陷波器**：N 取 20 的倍数时，滑动平均的零点精确落在 50/100/150/200 Hz，
免费覆盖工频和全部倍频，而且不依赖采样率精确等于 1 kHz。**`notch50.h` 可以删掉。**

### 2.2 副链（冲击 / 击打）—— 现在做不了

对单采样脉冲，各级平滑后的残余（A = 870 mV，σ = 311 mV）：

| 等效 N | 脉冲残余 | 噪声 σ | 脉冲 SNR |
|---|---|---|---|
| **1** | **870 mV** | **311 mV** | **2.80** |
| 5 | 174 mV | 139 mV | 1.25 |
| 10 | 87 mV | 98 mV | 0.88 |
| 100 | 8.7 mV | 31 mV | 0.28 |
| 500 | 1.74 mV | 13.9 mV | 0.13 |

**SNR 随 N 单调下降。** 冲击检测的最优解是不做平均、直接单点比较 —— 但单点 SNR 只有 2.8，
要做到可靠检测（误报率可接受）需要 5σ 以上，也就是需要 SNR ≥ 5。

**所以：击打路径必须先降噪，否则无解。** 见第 4 节。

---

## 3. 代码

```c
/* ================= 力/按压信号链  (fw 20260924) =================
 * 设计依据（实测于 Ozone_SecondTest / ThirdTest / FirstTest）:
 *   噪声: 白噪声 σ=311 mV, PSD 平坦到 500 Hz
 *   按压: +285 mV 向上, 起始沿 1.2 s, 释放 τ≈1.95 s, 持续 3.5 s
 *   按压本身很慢 -> 可以用极重的平滑而几乎不损失响应
 *   级联 20→25 等效 500 点: σ 316→9.7 mV, 群延迟 ≈250 ms
 *   50 Hz 及倍频落在滑动平均的精确零点上, 不需要额外陷波器
 */
#define FLT_N1        20u     /* 第1级: 1 kHz -> 50 Hz */
#define FLT_N2        25u     /* 第2级: 50 Hz 上再平均 -> 等效 500 点 */
#define FLT_BASE_A    0.002f  /* 基线跟踪系数 @50Hz, τ≈10 s */
#define FLT_VAR_A     0.002f  /* σ 估计系数 @50Hz */
#define FLT_SIG_MIN   0.003f  /* σ 下限, 防止阈值塌到 0 */
#define FLT_ON_K      6.0f    /* 按下阈值 = 6σ */
#define FLT_OFF_K     3.0f    /* 释放阈值 = 3σ */

static uint16_t flt_r1[FLT_N1], flt_r2[FLT_N2];
static uint32_t flt_s1, flt_s2, flt_c1, flt_c2, flt_warm;
static uint8_t  flt_i1, flt_i2;
static float    flt_var;

volatile float    flt_vin    = 0;   /* 滤波后电压 (V) */
volatile float    flt_base   = 0;   /* 基线 (V) */
volatile float    flt_dev    = 0;   /* 相对基线 (V), 按压为正 */
volatile float    flt_sigma  = 0;   /* 在线噪声 σ (V) */
volatile float    flt_thr    = 0;   /* 当前按下阈值 6σ (V) */
volatile uint32_t flt_press  = 0;   /* 1 = 按下 */
volatile uint32_t flt_ready  = 0;   /* 预热完成 */

/* 由 1 kHz SysTick 调用 */
void Filter_Tick1ms(void)
{
    uint16_t x = adc_raw[0];

    /* --- 第1级: 20 点滑动平均, 输出 50 Hz --- */
    flt_s1 += x - flt_r1[flt_i1];
    flt_r1[flt_i1] = x;
    flt_i1 = (uint8_t)((flt_i1 + 1u) % FLT_N1);
    if (++flt_c1 < FLT_N1) return;
    flt_c1 = 0;

    uint16_t y1 = (uint16_t)(flt_s1 / FLT_N1);

    /* --- 第2级: 25 点滑动平均 (滑动输出, 不是每 25 个才出一个) --- */
    flt_s2 += y1 - flt_r2[flt_i2];
    flt_r2[flt_i2] = y1;
    flt_i2 = (uint8_t)((flt_i2 + 1u) % FLT_N2);
    if (flt_c2 < FLT_N2) flt_c2++;

    flt_vin = (float)(flt_s2 / flt_c2) * (3.3f / 4095.0f);

    /* --- 预热: 填满第2级窗口后才允许检测 --- */
    if (flt_warm < FLT_N2) {
        if (++flt_warm == FLT_N2) { flt_base = flt_vin; flt_var = 4e-4f; flt_ready = 1; }
        return;
    }
    if (!flt_ready) return;

    float dev = flt_vin - flt_base;          /* 按压 -> 向上 -> 正 */
    float s   = sqrtf(flt_var);
    flt_dev = dev;
    flt_thr = FLT_ON_K * s;

    /* --- 按下期间 base 和 σ 都完全冻结 ---
     * 这一条很重要: 缓变的起始沿会在 |dev|<3σ 的区间里被当成"安静样本"
     * 吃进 σ 估计, 把 σ 抬高 60% 以上, 导致漏检。 */
    if (!flt_press)
    {
        if (fabsf(dev) < 3.0f * s)           /* 只用安静样本学习 */
        {
            flt_var  += FLT_VAR_A  * (dev * dev - flt_var);
            flt_base += FLT_BASE_A * dev;
        }
        if (dev > FLT_ON_K * s) flt_press = 1;
    }
    else
    {
        if (dev < FLT_OFF_K * s) flt_press = 0;
    }

    s = sqrtf(flt_var);
    if (s < FLT_SIG_MIN) s = FLT_SIG_MIN;
    flt_sigma = s;
}
```

### 3.1 必须同时改的两处符号

代码里现在假设"按压使电压**下降**"，和实测相反：

```c
/* main.c:249 */
static float adc_to_F(float voltage, float baseline)
{
  float f = voltage - baseline;   /* 改：按压【上升】 */
  if (f < 0) f = 0;
  return f;
}

/* main.c:415 */
press_dev[i] = vin_slow[i] - base[i];   /* 原来是 base[i] - vin_slow[i] */
```

### 3.2 可以删掉的

- `Core/Inc/notch50.h`（滑动平均的零点已经覆盖工频）
- `Core/Src/main.c` 里的 `osc_update` / `osc_floor` / `OSC_FLOOR_*`（那套"振荡幅度下界"判据
  是为四通道板设计的，单通道 + 现在的降噪水平下不再需要）

---

## 4. 验证结果

### 4.1 按压 SecondTest

| 方案 | 输出 σ | 6σ 阈值 | 事件峰值 | 峰值/阈值余量 | 检测延迟 | RAM |
|---|---|---|---|---|---|---|
| **现在的 EMA α=0.1** | **71.6 mV** | **430 mV** | 499 mV | **1.16×** | **1002 ms** | 0 B |
| 单级 N=100 | 28.0 mV | 168 mV | 338 mV | 2.0× | 326 ms | 200 B |
| 级联 20→10（等效 200） | 16.4 mV | 98 mV | 300 mV | 3.1× | 287 ms | 60 B |
| **级联 20→25（等效 500）** | **9.7 mV** | **58 mV** | **280 mV** | **4.8×** | **247 ms** | **90 B** |
| 级联 20→50（等效 1000） | 6.7 mV | 40 mV | 268 mV | 6.7× | 387 ms | 140 B |

**注意最反直觉的一点：级联 20→25 的检测延迟（247 ms）比现在的 EMA α=0.1（1002 ms）还短 4 倍。**
原因是阈值：现在代码的 6σ 阈值是 430 mV，得等按压爬到很高才越界；
新方案的阈值只有 58 mV，按压一开始就越界了。**更重的滤波反而更快。**

端到端检测结果：**检出 1 次按压，t = 3.799 … 7.299 s，持续 3.5 s，峰值 285 mV**，
静置段误触发 **0** 次。

### 4.2 按压信号的幅度保留

| 等效 N | 峰值保留 | 峰值/σ |
|---|---|---|
| 100 | 119% | 12.1 |
| 200 | 105% | 18.3 |
| **500** | **98%** | **28.9** |
| 1000 | 94% | 40.2 |
| 2000 | 90% | 76.2 |

N=500 只损失 2% 幅度，说明按压的频谱远比滤波器通带窄 —— 这就是"可以用重滤波"的量化依据。

### 4.3 击打 ThirdTest

新旧方案都检出 **0 个事件**（全程最大 3.0σ / 1.9σ，都在噪声范围内）。

---

## 5. 硬件：必须先做的事

### 5.1 击打路径无解，除非降噪

现在的噪声 σ = 311 mV，而击打（如果按 ThirdTest 里那个 870 mV 单采样脉冲算）SNR 只有 2.8。
要把 SNR 提到 5 以上，噪声必须降到 **175 mV 以下**，最好是 50 mV 量级。

最可能的原因和上一轮结论一致：

> `adc.c:69` 的 `SamplingTime = ADC_SAMPLETIME_1CYCLE_5`，**1.5 周期 = 107 ns**。
> STM32F0 采样保持电容约 8 pF，充到 12 位精度需要约 8.3τ，源阻抗上限 **≈1.6 kΩ**。
> 超过这个值，每次转换都是在读采样电容上的残留电荷 —— 结果就是平坦到奈奎斯特的白噪声。

改成：

```c
sConfig.SamplingTime = ADC_SAMPLETIME_239CYCLES_5;   /* 1.5 -> 239.5 周期 */
```

转换时间变成 252 周期 = 18 µs/通道。现在只用一路，拍 1 kHz 完全够（余量 55 倍）。

改完导一段 10 秒静置，看 σ 是否从 311 mV 掉下来。**如果完全不变，就是放大器自激** ——
98 倍增益直驱线缆和 ADC 引脚的容性负载很容易振，需要在输出串 50~100 Ω 隔离电阻，
并用示波器确认。

### 5.2 模拟抗混叠

放大器输出到 ADC 之间加 RC（1 kΩ + 100 nF，截止 1.6 kHz）。
比任何软件滤波都根本 —— 软件滤不掉已经折进来的东西。

### 5.3 击打的机械/电气通路要单独确认

ThirdTest 里击打完全没有信号，需要先排除：

1. 击打有没有真的打到传感器？（同样的位置、同样的力度，先用按压验证响应）
2. 击打时传感器输出端有没有波形？（示波器直接量放大器 OUT）
3. 如果放大器 OUT 有波形但 ADC 没读到 —— 那就是采样/带宽问题（5.1、5.2）
4. 如果放大器 OUT 也没有 —— 是传感器或机械耦合问题

---

## 6. 参数速查

| 参数 | 值 | 依据 |
|---|---|---|
| 输入噪声 σ | 311 mV（白） | 三份数据一致 |
| 级联 N1 / N2 | 20 / 25 | 等效 500，工频零点，90 B |
| 输出噪声 σ | 9.7 mV | 实测 |
| 降噪倍数 | 32× | 316 → 9.7 |
| 群延迟 | ≈250 ms | 实测检测延迟 247 ms |
| 基线跟踪 τ | ≈10 s @50 Hz | 远慢于按压的 1.95 s 释放 |
| 按下阈值 | 6σ ≈ 58 mV | 误触发 0 |
| 释放阈值 | 3σ ≈ 29 mV | 滞回 2:1 |
| 按压幅度 | +285 mV | 实测，余量 4.8× |
